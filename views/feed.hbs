<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>


    <h1>Todos los posts</h1>

    <form action="/search" method="get">
        <input type="text" name='text' placeholder="Pon tu name">
        <button type="submit">Buscar posts</button>
    </form>

    {{#each posts}}
        <p>{{this.text}}</p>
        <pre>{{this.user.name}} {{this.user.email}}</pre>
        <a href="/editar-post/{{this._id}}" style="padding: 15px 10px; background: green; color: white">Editar</a>
        <form action="/eliminar-post/{{this._id}}" method="POST">
            <button type="submit" style="padding: 15px 10px; background: red; color: white">Eliminar</button>
        </form>
        <hr>
    {{/each}}
    

    <a href="/crear-post">Crear otro tweet</a>
</body>
</html>